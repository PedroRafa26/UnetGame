import 'package:flutter/material.dart';

class LevelMain extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Este es el nivel'),)
    );
  }
}