import 'package:flutter/material.dart';

class Who extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Info about us'),),
    );
  }
}